export type LocaleStrings = {
  amen: string;
  after: string;
  the: string;
}

export const LOCALE_STRINGS : Record<string, LocaleStrings> = {
  en: {
    amen: "Amen",
    after: " after ",
    the: " the ",
  }
}